name: Django CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  SECRET_KEY: django-insecure-tvu5sey(pive7yf951p09#o-fgxxbmip1veoofvf7z4zb5k+8g
  EMAIL_HOST: smtp.gmail.com
  EMAIL_HOST_USER: 202305442mar.venkatswaroope3@gmail.com
  EMAIL_TLS: True
  EMAIL_FROM_EMAIL: test@nvshome.tk
  EMAIL_PORT: 587
  EMAIL_HOST_PASSWORD: 1235456
  FROM_MAIL: test@nvshome.tk
  DEBUG: True
  REQUEST: http://

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.11]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run migrations
      run: |
        python manage.py makemigrations
    - name: Run migrate
      run: |
        python manage.py migrate
    - name: Run test
      run: |
        python manage.py test
